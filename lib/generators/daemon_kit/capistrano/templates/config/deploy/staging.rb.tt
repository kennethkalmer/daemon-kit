#set :deploy_to, "/svc/<%= app_name %>" # defaults to "/u/apps/#{application}"
#set :user, "<%= app_name %>"            # defaults to the currently logged in user
set :daemon_env, 'staging'

set :domain, 'example.com'
server domain
